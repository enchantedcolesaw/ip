package gatsby;

import javafx.application.Application;

/** Starts the JavaFX version of Gatsby. */
public class Launcher {
    /** Launches Gatsby's JavaFX application. */
    public static void main(String[] args) {
        Application.launch(GatsbyGui.class, args);
    }
}
